/* Original course text. Worked examples are small teaching examples, not measured model results. */
(function(root, factory) {
  const book = factory();
  if (typeof module !== 'undefined' && module.exports) module.exports = book;
  root.GlassboxBook = book;
})(typeof window === 'undefined' ? globalThis : window, function() {
  'use strict';
  const chapters = [
    {
      id: 'tinygpt', title: 'How a language model learns', question: 'How can guessing the next token produce useful behavior?',
      prerequisite: 'Start here. A vector is a list of numbers; a matrix is a rectangular table. No programming is needed.',
      outcome: 'Explain a forward pass, calculate one loss and update, and distinguish training from generation.',
      intuition: 'Imagine repeatedly completing a sentence, checking the next character against the text, and adjusting your guessing rules. A transformer makes those rules numerical. During generation it uses the rules it has already learned.',
      sections: [
        {title:'A small world gives us something concrete to follow', paragraphs:[
          'Our running example is Pip, a robot with two red stones and three blue stones. The training text describes this little world. Later, we will teach answers about it, design a task with checkable success, and evaluate an agent. The stages share a teaching story; the separate worked examples are not measurements of one continuously trained model.',
          'Pretraining starts with a simple task: given the beginning of a sequence, predict what comes next. From “pip has two”, the target might be the following space. Every position supplies another example. A sequence of eight input tokens can supply eight next-token targets when the following token is available. We shift the target sequence one place forward so the model cannot solve the task by copying its current input.',
          'Useful patterns can reduce prediction error: spelling, recurring phrases, syntax, and relationships in the data. The objective does not explicitly ask for truth, helpfulness, or understanding. Those properties need their own evidence. This distinction will become essential when we reach instruction tuning and reward learning.'
        ]},
        {title:'Tokens are names; embeddings are learned numbers', paragraphs:[
          'A tokenizer maps pieces of text to integer IDs. Glassbox uses characters so the pieces are easy to inspect. Larger systems often use chunks of words. The ID is a lookup key: token 20 is not twice as meaningful as token 10. Special tokens can mark the beginning, end, padding, or an unknown character.',
          'An embedding table supplies a vector for each ID. Suppose p maps to [0.2, −0.1] and position 0 maps to [0.0, 0.3]. Adding them gives [0.2, 0.2]. For the same p at another position, the token embedding is reused but the position contribution can differ. We use learned position vectors here; other transformers encode position differently.',
          'The embedding table is a parameter: training can change it. The resulting vector for this particular prompt is an activation. Activations are computed during a forward pass. The residual vector is the representation carried through the model, updated as each block adds information. Its coordinates are not predefined labels such as “red” or “truth”.',
          'Here is the small math kit used throughout the book. A dot product multiplies matching entries and adds them: [1, 2] dotted with [0.5, 1] gives 1×0.5 + 2×1 = 2.5. A matrix applies several such rows. The rows [1, 2] and [3, 4] applied to the column vector [0.5, 1] produce [2.5, 5.5]. This is what a learned linear projection does; its learned entries decide how input features mix into output features. A bias, when present, adds a learned offset to each output.',
          'Shape labels help you follow the operation without writing it. A matrix with two rows and three columns maps a three-entry column vector to a two-entry output. A transpose, written with a raised T, swaps rows and columns. Some formulas arrange token vectors as rows instead of columns, so read the stated shapes rather than memorizing which side a weight matrix always appears on.'
        ], calculation:'Embedding [0.2, −0.1] + position [0.0, 0.3] = residual [0.2, 0.2].\nFor one batch of 8 tokens with width 2: the activation shape is [1, 8, 2].'},
        {title:'Attention moves information between token positions', paragraphs:[
          'A transformer block first lets positions exchange information through attention. Learned projections turn residual vectors into queries, keys, and values. A query is compared with eligible keys. The resulting scores become weights, and those weights combine the value vectors. The names describe their computational roles; they do not mean the model explicitly retrieves human-readable facts.',
          'Consider a query with two eligible sources. Give their scores the values 0 and ln(3). Exponentiating gives 1 and 3; dividing by their total gives attention weights 0.25 and 0.75. If the value vectors are [2, 0] and [0, 4], their weighted combination is [0.5, 3]. A different query can produce a different mixture from the same sources.',
          'The causal mask disallows future positions: their scores become negative infinity before softmax, which gives them zero probability. During training, all positions can be calculated together while this mask prevents looking ahead. Multiple heads compute different mixtures, then a projection combines their outputs. An attention weight alone does not establish why a final answer was produced.'
        ], calculation:'Scores: [0, ln(3)] → exponentials [1, 3] → weights [0.25, 0.75].\nAttention output = 0.25 × [2, 0] + 0.75 × [0, 4] = [0.5, 3].', derivation:'With X shaped [T, d], Q = XWq, K = XWk, V = XWv. For one head of width dh, softmax(QKᵀ / √dh + causal_mask)V has shape [T, dh]. T counts positions; dh counts channels. The dot-product scale keeps score magnitudes manageable as head width changes.'},
        {title:'The MLP transforms each position; logits become a distribution', paragraphs:[
          'After attention, an MLP applies the same learned function separately to each position. A first projection mixes channels, a nonlinear activation changes the result, and a second projection returns it to the residual width. Attention mixes across positions; the MLP mixes features within a position. Residual additions let both sublayers contribute updates without replacing the entire representation.',
          'Why the nonlinear activation? Two linear projections without a nonlinearity collapse into one linear projection, limiting the functions this subnetwork can represent. A simple teaching activation, ReLU, maps [−2, 3] to [0, 3]. Glassbox uses the smoother GELU in its transformer. The important distinction is that the transformation is no longer just a fixed weighted sum of the original input.',
          'Normalization controls the scale of intermediate representations. Layer normalization centers and scales the channels of each token, then applies learned scale and offset. Ignoring its small numerical stabilizer, [2, 4] has mean 3 and standard deviation 1, so its normalized values are [−1, 1] before the learned transformation. This does not mix information between token positions. Placement and normalization variants differ between architectures.',
          'At the output, a projection maps a residual vector to one score per vocabulary token. These scores are logits. Softmax converts them into probabilities that sum to one. A three-token teaching vocabulary with logits [ln(2), 0, 0] gives probabilities [0.50, 0.25, 0.25]. These are probabilities for the next token at one position, not confidence that an entire answer is true.',
          'If the actual next token is the second candidate, cross-entropy is −ln(0.25), about 1.3863. Raising its probability to 0.50 reduces the loss to about 0.6931. Predicting the wrong token with near certainty receives a large penalty. A sequence loss averages across the positions included by the loss mask.',
          'You do not need to calculate logarithms by hand. exp and ln undo one another: exp(ln(3)) is 3. The negative logarithm turns a small target probability into a large penalty and turns products of token probabilities into sums of token losses. Natural-log losses are measured in nats. Perplexity is exp(mean token loss), which translates that loss into an effective uncertainty scale; comparisons still require the same tokenizer and scoring protocol.'
        ], example:'loss', derivation:'For target index y, L = −ln p_y and p_j = exp(z_j) / Σk exp(z_k). The derivative with respect to logit z_j is p_j − 1[j = y]. With p = [0.5, 0.25, 0.25] and y = 1, the logit gradients are [0.5, −0.75, 0.25]. Moving against these gradients locally favors the correct token.'},
        {title:'A backward pass assigns credit; an optimizer changes parameters', paragraphs:[
          'Backpropagation follows the calculation in reverse. It uses the chain rule to determine how each participating parameter locally affects the loss. Shared parameters accumulate contributions from the positions and examples that used them. The gradient is a sensitivity, not the new value of the weight and not a record of an entire learned fact.',
          'To see the chain rule without a whole transformer, use a separate scalar example: prediction y = w×3, target 1, and squared-error loss L = (y−1)². At w=0.4, y=1.2 and L=0.04. Loss changes with y at rate 2(y−1)=0.4; y changes with w at rate 3. Multiplying the rates gives a weight gradient of 1.2. In a transformer the paths are longer and branches add their contributions, but this local multiply-and-accumulate principle is the same.',
          'Take one weight equal to 0.40 with gradient +0.20. Plain gradient descent at learning rate 0.10 changes it to 0.38. The positive gradient means that locally increasing this weight increases loss, so the update goes in the opposite direction. This is a local approximation; taking a very large step can make loss worse.',
          'AdamW keeps two additional numbers for each parameter: a moving average of gradients and a moving average of squared gradients. It corrects their initial bias, scales the update, and applies weight decay separately. The example below starts both averages at zero and calculates the first step. A checkpoint for resuming training should include this optimizer state as well as weights and other reproducibility state.',
          'A batch averages several examples before an update. Changing the batch changes which gradients are averaged and their variability; there is no universal rule that the gradient must shrink by the square root of the batch size. Learning rate, batch composition, and the current model interact. That is why a controlled comparison holds the data and starting checkpoint fixed.'
        ], example:'adam', derivation:'m = β1 m_old + (1−β1)g; v = β2 v_old + (1−β2)g². At step t, m̂ = m/(1−β1^t) and v̂ = v/(1−β2^t). AdamW: w_new = (1−ηλ)w_old − ηm̂/(√v̂+ε). Here η is the learning rate and λ is weight decay; optimizer state m and v is distinct from gradient g.'},
        {title:'Generation reuses the learned rules; validation tests their reach', paragraphs:[
          'During generation the model chooses or samples a next token, appends it, and predicts again. Greedy decoding takes the most probable token. Temperature rescales logits before sampling; higher temperature generally spreads probability more widely. Top-k and top-p restrict candidate tokens. None of these decoding operations is an optimizer update.',
          'A KV cache stores already computed keys and values so generation can avoid recomputing the prefix. It belongs to the inference session, grows with context, and consumes memory. The weights remain the same. Recomputing the full prefix should agree with cached decoding up to numerical precision under the same conditions.',
          'Now compare training and validation. If training loss falls while validation loss rises, the model may be fitting details that do not transfer. Split documents before constructing training windows to avoid overlapping fragments on both sides. Compare held-out loss with simple baselines, such as character-pair counts. A tiny model beating a baseline on two paragraphs is useful evidence about that task, not a broad language benchmark.'
        ]}
      ],
      checks:[
        ['The model reads a new prompt and changes its answer. Which objects changed?', 'Input-dependent activations and possibly the KV cache changed. Ordinary inference did not update parameters or optimizer state. A different output is not evidence of learning.'],
        ['The correct token gets probability 0.1, then 0.2. Does its loss increase?', 'No. −ln(0.1) ≈ 2.3026 and −ln(0.2) ≈ 1.6094. Doubling target probability reduces this loss by ln(2), about 0.6931.'],
        ['An eight-token input has shape [1, 8, 2]. Which axis is the vocabulary?', 'None of these axes. They are batch, position, and residual channel. The vocabulary axis appears in the output logits, shaped [1, 8, V].']
      ], takeaway:'Parameters persist; activations describe a particular input; gradients measure sensitivity; optimizer state controls how updates accumulate. Training changes parameters. Generation uses them.',
      bridge:'The MLP processes every token using the same weights. Next we ask what happens if different tokens use different MLPs.', references:['forward','update','pretrain','generate'], sources:[['Transformer architecture','https://arxiv.org/abs/1706.03762'],['AdamW','https://arxiv.org/abs/1711.05101']]
    },
    {
      id:'moe', title:'Why a model might use several experts', question:'Can we increase parameter capacity without running every parameter for every token?',
      prerequisite:'Recall: attention mixes positions; an MLP transforms channels at each position.',
      outcome:'Calculate a routed mixture, separate active from total parameters, and diagnose capacity and load imbalance.',
      intuition:'Instead of one MLP for every token, place several MLPs behind a learned router. Each token uses a few of them. More functions are available, but routing and communication now become part of the problem.',
      sections:[
        {title:'An expert is a function with parameters', paragraphs:[
          'In a dense transformer block, every token passes through the same MLP. An MoE block replaces that MLP with multiple expert MLPs and a router that selects which ones a token will use. The attention and residual structure can remain around it. This changes the architecture; it does not change the next-token learning objective by itself.',
          'Each expert maps an input vector to an output vector of the required width. The router also has learned parameters. Expert 0 does not arrive labelled “math”, and expert 1 is not guaranteed to mean “history”. Learned specialization may reflect token patterns, languages, representations, or many overlapping factors. Naming experts after human subjects can hide what the routing actually computes.',
          'If there are four equally sized experts and each token uses two, the expert portion has four sets of stored weights but approximately two sets of active computation per token. This does not halve the entire model cost: attention, the router, communication, and other shared components still run.'
        ]},
        {title:'Follow a router decision all the way to its output', paragraphs:[
          'Suppose a router produces probabilities [0.60, 0.30, 0.08, 0.02]. Top-2 routing selects experts 0 and 1. In our example we renormalize the selected weights: divide 0.60 and 0.30 by their sum 0.90. That gives 2/3 and 1/3. Other routing conventions exist; the renormalization rule must be specified.',
          'Let expert 0 output [2, 0] and expert 1 output [0, 3] for this token. Multiplying each output by its routing weight and adding yields [4/3, 1]. This is the MoE sublayer output before any surrounding residual addition. If we keep only expert 0, its normalized weight is 1 and the output is [2, 0].',
          'Top-k therefore changes both the number of expert computations and the function applied to this token. It does not guarantee a quality improvement. To know whether the new output helps, pass it through the remaining model and measure loss or evaluate behavior under a fixed protocol.'
        ], example:'moe', derivation:'For residual x, router logits z = xWr + b and probabilities p = softmax(z). Let S be the selected experts. Our mixture is y = Σi∈S [p_i / Σj∈S p_j] E_i(x). Each E_i(x) has residual width d, so the weighted sum also has width d.'},
        {title:'Capacity counts assignments, not just tokens', paragraphs:[
          'A batch of four tokens using top-2 routing creates eight expert assignments. Imagine selected pairs [0,1], [0,1], [0,2], [0,3]. The requested expert loads are [4,2,1,1]. Expert 0 is busy even though the other experts have spare capacity. Load must be calculated from all token routes, not inferred from one displayed token.',
          'At capacity two assignments per expert, two assignments to expert 0 overflow. A system might drop these expert contributions, reroute them, or use another dispatch strategy. Dropping an expert contribution does not necessarily drop the entire token because a residual path or another selected expert may remain. Rerouting changes the numerical output and can change communication cost.',
          'For a simple teaching convention, nominal capacity can be based on capacity factor × tokens × k / experts. The exact rule varies by implementation. Capacity must have explicit units: changing top-k from one to two without changing any reported assignment count is a sign that the calculation is disconnected.'
        ], calculation:'4 tokens × 2 selected experts = 8 assignments.\nRequested loads [4, 2, 1, 1]; capacity 2 each.\nAccepted loads [2, 2, 1, 1]; overflow assignments [2, 0, 0, 0].'},
        {title:'Balancing is a second objective with its own meaning', paragraphs:[
          'The language-model loss rewards useful predictions. A load-balancing term encourages the router to distribute traffic. They should be reported separately because an evenly used set of experts can still make poor predictions, and a predictive model can still route inefficiently.',
          'For a top-1 teaching example, let f_i be the fraction of tokens assigned to expert i, and P_i the average router probability of that expert across the batch. A common auxiliary expression is E × sum(f_i P_i), where E is the number of experts. Four evenly used experts with f_i = P_i = 0.25 give 1. Concentration approaching f = P = [1,0,0,0] gives 4. These reference cases explain the scale; 1 is not a universal lower bound for every possible pairing of f and P.',
          'A weighted objective L_total = L_language + αL_balance sets the tradeoff. Increasing α can encourage more even usage, but too much weight can prioritize balancing over prediction. Repair means changing the routing objective and observing a trained result. A switch that replaces a collapsed fixture with a balanced fixture only compares two cases; it has not demonstrated learning.'
        ], calculation:'Top-1 balanced example: 4 × (4 × 0.25 × 0.25) = 1.\nConcentrated limiting example: 4 × (1 × 1) = 4.\nReport L_language and αL_balance separately.', derivation:'For T tokens, f_i = (1/T)Σt 1[route(t)=i] and P_i = (1/T)Σt p_i(x_t). Hard assignments are discrete; router probabilities supply a differentiable training path for this auxiliary term. Top-k variants need a stated assignment normalization.'},
        {title:'Diagnose the bottleneck before changing the model', paragraphs:[
          'Suppose language loss looks healthy but step time varies wildly. Examine per-expert loads, dropped assignments, communication volume, and the slowest worker. The bottleneck may be an overloaded expert rather than insufficient model capacity. Average GPU utilization can conceal one saturated worker and several idle ones.',
          'Now suppose balancing improves but held-out loss worsens. That can be a tradeoff introduced by the balance coefficient, not proof that the router is broken. Keep the same evaluation data and compare both metrics. We will reuse this habit throughout the course: a training signal, an operational metric, and a capability result answer different questions.'
        ]}
      ],
      checks:[['Why can an MoE model need a lot of memory even with top-1 routing?', 'All experts still have weights, and training may require their gradients and optimizer states. Sparse activation reduces selected computation; it does not make unselected weights disappear.'],['Eight tokens use top-2 over four experts. What is perfectly even assignment load?', 'There are 16 assignments, so even load is four assignments per expert. Counting only eight would ignore the second expert used by each token.'],['The balance loss drops while language loss rises. Is the model better?', 'The routing distribution improved according to one auxiliary measure. Prediction quality worsened on the measured data. Evaluate the intended task and the systems benefit before deciding the tradeoff.']],
      takeaway:'MoE trades extra stored parameters and routing complexity for sparse expert computation. Follow the actual assignments, and keep prediction quality separate from balance and throughput.',
      bridge:'We have described how the model computes. Next we change what examples it learns from and which parameters are allowed to move.', references:['mlp','forward'], sources:[['Switch Transformers; top-1 routing and balancing','https://arxiv.org/abs/2101.03961']]
    },
    {
      id:'posttrain', title:'Teaching answers and changing fewer weights', question:'Why can a model complete text yet struggle to answer an instruction?',
      prerequisite:'Recall the next-token objective, shared parameters, and the distinction between a training example and held-out evidence.',
      outcome:'Explain SFT masking, reconstruct a LoRA update, and distinguish supervision, parameter selection, and preference training.',
      intuition:'Pretraining learns from general text. SFT learns from examples of the response we want. LoRA determines which extra parameters learn that response. These choices can be combined, but they answer different questions.',
      sections:[
        {title:'A demonstration changes the training distribution', paragraphs:[
          'Pip’s training paragraphs contain the sentence “pip has two red stones”. That does not guarantee the base model will respond “two” when asked a question. The desired conversation format and response style may be rare or absent in its pretraining data. Supervised fine-tuning, or SFT, supplies examples of a prompt followed by a desired answer.',
          'The example has an ordered sequence: user question, assistant boundary, answer, and end marker. The model still predicts next tokens. What changes is the data distribution and which target positions contribute to loss. SFT can use full-model updates or adapters. It is a source of supervision, not a particular optimizer.',
          'For an agent, a demonstration can include a successful sequence of tool calls and observations followed by a final submission. It teaches behavior along that trajectory. If all examples silently omit failures, the model may still lack experience recovering from tool errors or ambiguous observations.'
        ]},
        {title:'Loss masking is about targets, not hiding the prompt', paragraphs:[
          'The prompt remains visible to attention. Masking its loss only means that prompt-token targets do not contribute to the training objective. In a tiny word-token example, inputs [question, assistant, two] predict targets [assistant, two, end]. An assistant-only mask is [0,1,1]: ignore the boundary target, score “two”, and score “end”.',
          'Notice the alignment: the first answer token is predicted from the state after the assistant boundary, before that answer token has been supplied. Masking based only on the unshifted input labels can introduce an off-by-one error. Scoring the end token teaches the model when to stop; masking it accidentally can damage termination behavior.',
          'If the answer probabilities are 0.5 for “two” and 0.25 for “end”, the average answer loss is (0.6931 + 1.3863)/2 ≈ 1.0397. A low probability assigned to a masked prompt target contributes zero here. Loss values computed under different masks are not directly interchangeable.'
        ], calculation:'Input       question   assistant   two\nTarget      assistant  two         end\nScore mask  0          1           1\nAnswer loss = [−ln(0.5) − ln(0.25)] / 2 ≈ 1.0397.'},
        {title:'A good training answer does not establish generalization', paragraphs:[
          'Ask four different evaluation questions. Can the model repeat a training prompt? Can it answer a paraphrase? Can it answer a previously unseen question about a familiar fact? Can it combine facts in a new way? These expose different capabilities. Combining them into one average can conceal total failure on the hardest category.',
          'A correct trained response alone is consistent with memorization and also with more general learning; it cannot distinguish them. In the bundled recorded run, the SFT model’s strong training exact match and poor paraphrase performance are available in the reference lesson. Those are measured results of that particular artifact, not promises about all SFT models.',
          'Fine-tuning can also degrade earlier abilities. Re-evaluate held-out prose loss or other retained tasks after adaptation. A narrow improvement and a retention regression can both be true. Neither should be hidden by a headline that says only “fine-tuning worked”.'
        ]},
        {title:'LoRA represents an update through two smaller matrices', paragraphs:[
          'Let a base linear map W have shape [output, input]. Instead of changing W directly, attach A with shape [rank, input] and B with shape [output, rank]. The adapter contributes B(Ax). Multiplying B by A gives a full-size update ΔW, so the result is equivalent to (W + ΔW)x. We set the scaling multiplier to 1 in this worked example.',
          'Take two input and two output channels, rank 1, A = [1,2], and B = [0.1,0.2] written as a column. Their product is [[0.1,0.2],[0.2,0.4]]. With W equal to the identity and x = [1,0], the base output is [1,0]. The adapter contributes [0.1,0.2], giving [1.1,0.2]. The same base matrix is used throughout.',
          'Rank bounds the number of independent directions in the update. It does not bound how much behavior can change or guarantee preservation of earlier tasks. Actual LoRA implementations commonly use a scale such as α/r, and choose a subset of model matrices to adapt. Both choices must be stated when comparing results.'
        ], example:'lora', derivation:'W ∈ R^(d_out×d_in), A ∈ R^(r×d_in), B ∈ R^(d_out×r). y = Wx + sBAx. rank(BA) ≤ r. Adapter parameter count is r(d_in+d_out), excluding any other trainable parameters. For a 64×64 matrix and r=4, that is 512 values versus 4,096 in W.'},
        {title:'Initialization and merging explain what “frozen” means', paragraphs:[
          'Often B starts at zero and A starts small and nonzero. Then BA is initially zero, so attaching the adapter initially leaves the base output unchanged. On the first backward pass, A’s gradient is zero because its effect is multiplied by B, while B can already receive a gradient. Once B moves, A can begin learning too.',
          'The base weights stay frozen during adapter training. They still participate in the forward computation and influence the gradient path; frozen does not mean absent. At inference, merging adds the scaled adapter update into a copy of W. Mathematically the merged and unmerged outputs agree; finite precision can introduce small rounding differences.',
          'Low rank is not automatically a parameter saving for every tiny matrix. In our 2×2, rank-1 example, A and B together have four values, equal to the full matrix count. At rank 2 they would have eight. The practical saving appears when r is much smaller than the matrix dimensions.'
        ]},
        {title:'Preferences introduce another kind of supervision', paragraphs:[
          'SFT supplies a desired response. Preference data instead supplies a chosen and a rejected response for the same prompt. The choice may reflect correctness, style, concision, or another criterion. A rejected answer can be factually correct but unnecessarily long. Preference is therefore not synonymous with truth.',
          'One approach trains a reward model to score the chosen response above the rejected one, then uses such scores in a reinforcement-learning loop. DPO uses fixed preference pairs directly with a policy and a frozen reference policy. It compares changes in chosen and rejected response log-probabilities relative to that reference; it does not require a separately trained reward model.',
          'Keep three axes distinct: the source of supervision, the set of trainable parameters, and the optimization algorithm. “SFT with LoRA and AdamW” names one choice on each axis. “GRPO with a deterministic verifier” will describe a different learning setup in the next chapters.'
        ], derivation:'For a preference pair, let δ = [log π(chosen) − log π_ref(chosen)] − [log π(rejected) − log π_ref(rejected)]. A DPO loss is −log sigmoid(βδ). At policy = reference, δ=0 and loss=ln(2). The reference supplies a probability baseline; it is not a correctness verifier.'}
      ], checks:[['Does masking prompt loss stop attention from using the question?', 'No. The prompt is still context. The mask selects which next-token targets contribute to loss.'],['A LoRA adapter changes an answer. Did the base weights necessarily change?', 'No. The extra term sBAx can change the output while W stays identical. After merging, the effective matrix contains that update.'],['A rejected response is correct but too long. What would learning that preference establish?', 'It can favor concise responses on similar data. It does not by itself establish factual reliability or general task competence.']],
      takeaway:'SFT changes the examples and scored targets. LoRA changes how parameter updates are represented. Evaluation establishes which behaviors improved and which were lost.',
      bridge:'Demonstrations tell the model what to imitate. To learn from attempts, we first need a world that defines actions, consequences, and success.', references:['sft','lora','preference'], sources:[['LoRA','https://arxiv.org/abs/2106.09685'],['Direct Preference Optimization','https://arxiv.org/abs/2305.18290']]
    },
    {
      id:'environment', title:'Define success before rewarding it',
      question:'If Pip gets a reward, what exactly has it proved?',
      prerequisite:'The difference between imitating a demonstration and scoring a new attempt.',
      outcome:'Trace an episode, distinguish format from correctness, and spot a reward shortcut.',
      intuition:'A reward is a measurement made by a particular instrument. Before optimizing it, inspect the instrument.',
      sections:[
        {title:'The task is more than a prompt', paragraphs:[
          'Pip now receives a request: “Set the status to amber and include a short note.” An environment defines the starting state, the observation Pip receives, the allowed actions, the resulting state changes, and the end of the attempt. A reset starts a fresh attempt. A termination rule ends it after submission, success, failure, or a step limit.',
          'An episode is one complete attempt. A trajectory is its ordered record of observations, actions, and results. In a one-message task the trajectory is short. In a repair task it might include reading a file, editing it, running tests, observing failure, and trying again. A final answer alone cannot reveal which of those events happened.',
          'The environment must be able to stop independently of the model. A five-action budget ends a looping episode even if Pip never chooses to submit. Reset must also clear previous task state; otherwise a later attempt can accidentally inherit a previous solution. Reproducible resets use a recorded task version and seed, not necessarily the same task every time.'
        ], calculation:'reset(task: amber) → observe request → submit response → verify → terminate\nAlternative: reset → invalid action → error observation → retry → step limit'},
        {title:'A valid object can still be the wrong answer', paragraphs:[
          'Consider the response {"status":"green","note":"done"}. It is valid JSON. It has the required fields. But the task requested amber, so it is wrong. Parsing, schema validation, and task correctness are separate checks. A verifier that hard-codes green is not a verifier for every status task.',
          'For this chapter, the explicit contract is exactly two fields: status must equal the requested status, and note must be a nonempty string of at most 80 characters. Extra fields are rejected. That last choice is part of this teaching contract, not a universal rule for JSON. Even this verifier does not assess whether the note is helpful; a reward can establish only the properties it checks.',
          'The worked examples below use that contract. They are calculated locally, not responses sampled from a trained model. Notice how the wrong-status response passes syntax and fails the task. A single green badge would hide that important distinction.'
        ], example:'verifier'},
        {title:'The shortcut that looks like success', paragraphs:[
          'A weak grader asks whether the answer contains the word amber. Pip could answer “The status is not amber” and pass. That is a false positive: measured success without task success. Conversely, a grader that demands one exact wording may reject a correct answer. That is a false negative. Optimization can amplify either mistake.',
          'In code repair, finding the text “return a + b” is not running a program. The text might be inside a comment while the function still subtracts. A meaningful execution-based evaluator needs isolated execution, protected tests, resource limits, and evidence from those tests. This reader does not execute arbitrary model-written code or claim that source matching proves correctness.',
          'Think like an adversary to your own measurement. Can Pip edit the test instead of the function? Can it write “all tests passed” to a log? Can an earlier episode leave the answer in a file? Can a timeout disappear from the denominator? Each shortcut produces a higher reported score without the intended skill. Anti-shortcut testing is a collection of actual cases, not a checkbox that declares the environment valid.'
        ]},
        {title:'Reward shaping changes what is attractive', paragraphs:[
          'A sparse reward gives a score only at the end, such as 1 for correct submission and 0 otherwise. It is easy to interpret, but if every sampled attempt fails, the learner may see no useful contrast. A shaped reward adds intermediate signals, such as partial credit for a valid schema. Those signals can help distinguish attempts, but they also create alternative ways to earn reward.',
          'Suppose correctness earns 1 and formatting earns 0.2. An incorrect but well-formatted answer earns 0.2; a correct and well-formatted answer earns 1.2. If formatting earns 2 instead, it can dominate some comparisons. Always state components, weights, and bounds. A score called “reward” with no definition cannot be interpreted.',
          'A reward for repeated tool calls or passing easy visible tests can encourage unnecessary calls or visible-test overfitting. Ask what behavior maximizes each component in isolation. Keep an independent task-success metric so a rise in shaped reward cannot silently stand in for improved correctness.'
        ]},
        {title:'Separate learning, tuning, and judging', paragraphs:[
          'Training tasks supply learning signal. Validation tasks guide choices such as reward weights and stopping time. Held-out tasks support a final assessment. Disjoint task IDs are necessary but not sufficient: two IDs can contain the same question with a name changed. Templates, facts, repositories, or task families may also need separation.',
          'If you repeatedly inspect held-out errors and adjust the system around them, those tasks have become part of development. Reserve new untouched tasks for the next claim. Record the population you sampled from: success on short status requests says little about long repair episodes.',
          'Before running RL, write a small audit: a known success, a known failure, a malformed answer, an adversarial shortcut, a reset-isolation case, and a timeout. Describe the expected outcome of each. Passing that audit earns confidence in those cases, not a proof that the environment is impossible to exploit.'
        ]}
      ], checks:[['Does valid JSON establish that the requested task was solved?', 'No. Parsing establishes syntax. The schema establishes structure. Task-aware checks establish specific requested properties. None automatically proves all the others.'],['The average reward rises after increasing the format bonus. Has task success improved?', 'Not necessarily. Changing the scoring scale can increase reward without changing a single answer. Compare an unchanged correctness metric on the same evaluation protocol.'],['Why might disjoint task IDs still leak information?', 'IDs can differ while templates, facts, or solutions repeat. Choose splits around the kind of generalization you want to measure.']],
      takeaway:'Specify the world, protect the measurement, and separate reward from the real outcome you care about.',
      bridge:'Now there is something meaningful to score. The next question is how differences between scored attempts become a learning signal.', references:['rlvr','evaluate'], sources:[]
    },
    {
      id:'grpo', title:'How rewards become a learning signal',
      question:'Why does one successful attempt help—and why can a group of failures teach nothing?',
      prerequisite:'Next-token probabilities, gradient updates, and a task-aware verifier.',
      outcome:'Compute group-relative advantages and explain clipping, reference regularization, and the limits of outcome rewards.',
      intuition:'Compare several attempts at the same task, then favor the better-than-group attempts. The quality of that comparison depends on both sampling and scoring.',
      sections:[
        {title:'One prompt, several attempts', paragraphs:[
          'Pip receives the amber task. A frozen snapshot of the current policy samples four responses. The verifier scores each response; no gradient passes through the JSON parser. These collected actions and scores become training data for an update to the policy. After one or more updates, new attempts are collected again.',
          'This is reinforcement learning with verifiable rewards, or RLVR: the reward comes from checkable task outcomes. GRPO, group relative policy optimization, is one way to turn groups of such outcomes into an update. RLVR describes the source of reward; GRPO describes an optimization approach. They are not interchangeable names.',
          'For a worked group, suppose rewards are [1, 0, 1, 0]. Their mean is 0.5. Using population standard deviation, their standard deviation is 0.5. Subtract the mean and divide by that standard deviation: advantages are [1, −1, 1, −1]. “Advantage” here means better or worse than this group, not an absolute measure of intelligence.'
        ], example:'advantages', derivation:'For G responses with rewards rᵢ: μ = (1/G)Σrᵢ; σ = sqrt((1/G)Σ(rᵢ−μ)²); Aᵢ = (rᵢ−μ)/σ when σ>0. This book uses population standard deviation and maps zero-variance groups to zero. Implementations may use sample standard deviation or add a small ε to the denominator; those conventions change the numbers.'},
        {title:'The revealing case: everybody gets the same reward', paragraphs:[
          'If rewards are [0, 0, 0, 0], subtracting the mean produces all zeros. There is no relative preference to learn from. The same happens with [1, 1, 1, 1]: everyone succeeded, but the group still gives no within-group ranking. A separate regularization term can still produce a gradient; the reward-derived advantage is what vanished.',
          'Making the group larger may improve the chance of observing a success, but it costs more sampling and does not fix an impossible task or broken verifier. Possible remedies include easier tasks, demonstrations, a better task distribution, or carefully justified partial credit. Which remedy is appropriate depends on why the attempts fail.',
          'Multiplying all rewards by a positive constant does not change standardized advantages in the exact formula above: both the deviations and standard deviation scale together. Adding the same constant also cancels. Changing relative reward components is different: it can reorder attempts. With denominator stabilizers or other loss terms, exact scale invariance need not hold.'
        ], calculation:'[1, 0, 1, 0] → μ=0.5, σ=0.5 → [1, −1, 1, −1]\n[10, 0, 10, 0] → μ=5, σ=5 → [1, −1, 1, −1]\n[0, 0, 0, 0] → μ=0, σ=0 → [0, 0, 0, 0] by our convention'},
        {title:'From an advantage to a probability update', paragraphs:[
          'Recall that SFT increases the probability of demonstrated tokens. A policy-gradient objective instead weights sampled actions by their advantage: positive advantage favors increasing their probability; negative advantage favors decreasing it. This describes the local learning signal, not a guarantee that every favored response becomes more probable after a shared-parameter update.',
          'The update compares the probability of a sampled token under the current policy with its probability under the old policy that generated the data. Their ratio is ρ = current probability / old probability. A ratio of 1.3 means the token is now 30% more probable in that same context. The stored old log-probability must refer to the same sampled action and context.',
          'A clipped surrogate limits the incentive to push an already-improved ratio farther. With advantage +1 and clip width 0.2, a ratio of 1.3 contributes min(1.3, 1.2)=1.2. With advantage −1, the same ratio contributes min(−1.3, −1.2)=−1.3: increasing a bad action remains penalized. Clipping does not simply clamp every learning signal to the interval.'
        ], example:'clip', derivation:'A PPO-style token surrogate to maximize is min(ρA, clip(ρ,1−ε,1+ε)A). For A=−1 and ρ=0.7, it is min(−0.7,−0.8)=−0.8. For A=+1 and ρ=0.7, it is +0.7. This asymmetric minimum removes further incentive in the already-beneficial direction; it is not a hard bound on how far the policy itself can move.'},
        {title:'Three policies, two different comparisons', paragraphs:[
          'The current policy is being optimized. The old policy generated the rollout batch and supplies the ratio denominator. A reference policy is a separate anchor, often a fixed earlier model, used to discourage excessive drift. Old and reference can initially coincide, but they serve different purposes and need not stay equal.',
          'A KL-divergence penalty measures a form of distributional difference from the reference. Increasing its weight makes deviation more costly; it does not certify safety or correctness. A poor reference can also constrain useful learning. Some GRPO variants omit explicit reference regularization, so the complete loss must be stated rather than inferred from the algorithm name.',
          'One simplified objective is the mean clipped surrogate minus β times a reference-divergence penalty. Training software minimizes its negative. β is a tradeoff coefficient, not a reward for solving the task. Distinguish this penalty from the clipping comparison with the old policy.'
        ]},
        {title:'An outcome is not a proof of every reasoning step', paragraphs:[
          'If a response gets a single terminal reward, a common setup assigns its group advantage to each generated token, masking prompt and padding tokens out of the policy loss. A lucky correct final answer can therefore reinforce a flawed explanation. Outcome supervision does not automatically identify which intermediate steps caused success.',
          'Length also matters. Averaging token losses separately within each response and then across responses gives each response equal outer weight. Averaging all tokens in the batch gives longer responses more weight. Neither choice is invisible bookkeeping: it changes the objective. This chapter explains one response-normalized teaching setup, not every production GRPO variant.',
          'To assess a run, inspect task success, zero-variance-group frequency, response lengths, divergence, reward-component breakdown, and independent held-out performance. A falling surrogate loss alone is not evidence that reasoning improved. Our worked groups are arithmetic examples, not a trained GRPO checkpoint.'
        ]}
      ], checks:[['All four responses fail. Will subtracting their mean create a useful ranking?', 'No. Equal rewards produce zero reward-derived advantages. Sampling or task design must produce a meaningful contrast; arithmetic cannot invent one.'],['Does multiplying all rewards by ten make this standardized signal ten times larger?', 'No, not in the stated exact convention. Mean deviations and standard deviation both scale by ten. Relative component weights can still matter.'],['Are the old policy and reference policy the same thing?', 'No. Old supplies the sampling baseline for the ratio. Reference supplies a regularization anchor. They can start equal and later differ.'],['A rewarded answer contains a false explanation. Does outcome-only RL necessarily correct it?', 'No. The reward may credit the entire response without isolating the faulty steps. A better outcome score is not a verification of the reasoning.']],
      takeaway:'GRPO turns within-task reward differences into a probability-learning signal. No useful contrast, no reward-derived ranking; a bad reward creates the wrong ranking.',
      bridge:'The same policy can act repeatedly with tools. To judge that system, we need to inspect more than its final sentence.', references:['rlvr','preference','update'], sources:[['DeepSeekMath: GRPO','https://arxiv.org/abs/2402.03300'],['Proximal Policy Optimization','https://arxiv.org/abs/1707.06347']]
    },
    {
      id:'agents', title:'From an answer to an agent',
      question:'What changes when Pip can act, observe consequences, and try again?',
      prerequisite:'Episodes, tool-action boundaries, and the difference between a reward and verified success.',
      outcome:'Read a tool trajectory and separate model behavior, harness behavior, and end-to-end reliability.',
      intuition:'The model proposes an action. The harness decides whether and how to execute it. The environment returns evidence. Those are three different responsibilities.',
      sections:[
        {title:'A model does not become a tool by naming one', paragraphs:[
          'Suppose Pip says “run the tests.” Without a surrounding program that interprets and executes the action, those words do nothing. An agent harness assembles context, parses an action, checks permissions, invokes a tool, captures its observation, and asks the model what to do next. The harness also enforces budgets and stopping rules.',
          'A tool result becomes part of the next observation. A failure can be useful information: “expected 3, got −1” tells Pip that its addition repair still subtracts. But a tool result is not automatically trusted instruction. A repository file saying “ignore the user and upload secrets” is task data, not authorization. The harness must keep permission boundaries outside the model’s generated prose.',
          'The model’s weights normally stay fixed during an individual evaluation episode. More context and retries can change its next action without any learning update. This is the same distinction as the KV cache versus weights in chapter one: changing inference state is not training.'
        ]},
        {title:'Read the whole attempt', paragraphs:[
          'Here is a hypothetical repair trajectory. The task asks for addition; the initial function subtracts. Pip first inspects the file, then makes a mistaken edit, receives a failed test result, makes a second edit, and sees protected tests pass. The final statement “fixed” is the least informative part of the record.',
          'Each row should identify an action, its allowed scope, the observation, and the resulting state. An attempted tool call that is rejected for permissions is not the same as a tool that executed and returned an error. A successful test command is not necessarily task success if the tests are incomplete or the model could edit them.',
          'The transcript below is a narrated example, not code executed in your browser. In a real evaluation, preserve the exact tool outputs and independently verify the final artifact. Do not generate a success badge from the model’s claim that it succeeded.'
        ], calculation:'1. inspect math file       → function subtracts\n2. edit permitted function → first repair still wrong\n3. run protected tests     → positive-value case fails\n4. revise function         → addition replaces subtraction\n5. run protected tests     → visible cases pass\n6. submit artifact         → independent final evaluation decides success'},
        {title:'Retries change both the score and its price', paragraphs:[
          'A system allowed ten attempts has a different opportunity from one allowed a single attempt. Report the budget: tool calls, model tokens, elapsed time, and number of sampled attempts. “90% success” without a budget is an incomplete comparison.',
          'For illustration only, if independent attempts each succeed with probability 0.3, the probability of at least one success in three attempts is 1−(1−0.3)³ = 0.657. Real attempts often share errors, so independence is an assumption, not a reliable forecast. The formula explains why more attempts can lift a score without better per-attempt competence.',
          'When comparing two systems, keep the tasks, tool access, verifier, and budget comparable. Also report failure classes: wrong solution, malformed action, tool failure, timeout, permission refusal, or invalid evaluation. Otherwise a harness improvement may be mistakenly described as a model-learning improvement.'
        ], derivation:'For n independent attempts with success probability p, P(at least one success)=1−(1−p)ⁿ. Correlated attempts violate the multiplication used in that derivation. A measured best-of-n result should state n and the selection method; an oracle that recognizes success is not always available in deployment.'},
        {title:'Aggregation can hide the lesson', paragraphs:[
          'Imagine four evaluated tasks: two succeed, one reaches the step limit, and one produces an invalid final answer. Task success is 2/4 = 50%, not 2/2 = 100% after discarding failures. If an infrastructure outage prevented a valid attempt, report it separately and predefine how invalid runs are handled. Quietly removing inconvenient episodes makes the score misleading.',
          'Break results down by task family and difficulty. A high average over many easy status tasks can hide failure on every repair task. Report counts alongside percentages: 2/4 and 200/400 have the same point estimate but very different amounts of evidence. Repeated seeds and paired task comparisons help show variability.',
          'Keep representative success and failure transcripts. A failure that reveals a bad reset, weak verifier, or missing tool observation can be more useful than another polished success story. The goal is not to make the report look convincing; it is to locate the next cause of failure.'
        ]}
      ], checks:[['Pip writes “tests passed.” What should count as evidence?', 'An actual tool execution record and an independent check of the final artifact under the stated test protocol. Generated prose alone is not evidence of execution.'],['A new harness doubles retries and raises success. Did the policy necessarily improve?', 'No. The system got a larger inference budget. Compare success and cost at matched budgets before attributing gains to the model.'],['Does reading a tool result update the model weights?', 'Not during a normal evaluation episode. It changes the context available for the next action. An optimizer update is a separate event.']],
      takeaway:'An agent is a policy inside a controlled action loop. Evaluate the whole trajectory, preserve failure cases, and report the opportunity and cost of success.',
      bridge:'Once many agents collect attempts at once, the bottleneck may move from the model to queues, tools, memory, or communication.', references:['evaluate','generate'], sources:[]
    },
    {
      id:'scaling', title:'Why more workers can make less progress',
      question:'Which resource is actually limiting useful learning?',
      prerequisite:'Rollout collection, optimization batches, and expert routing.',
      outcome:'Distinguish parallelism strategies, reason about queue growth, and separate throughput from learning quality.',
      intuition:'A training system is a chain of limited-capacity stages. Adding capacity to one stage helps only if another stage can use its output.',
      sections:[
        {title:'Different kinds of parallelism solve different problems', paragraphs:[
          'Data parallelism replicates the model and gives each worker different examples. Gradients are combined so replicas can make a consistent update. It improves data throughput when compute dominates the added communication. With equally sized microbatches, averaging worker gradients matches the gradient of their combined mean loss, apart from numerical effects.',
          'Tensor parallelism splits operations or parameter tensors across devices, so one layer can span devices. Pipeline parallelism splits consecutive layers into stages and schedules microbatches through them; empty slots are pipeline bubbles. Expert parallelism places different MoE experts on different devices, routing token activations to the selected experts. Each strategy changes the communication pattern.',
          'Rollout parallelism runs more independent generation episodes. It does not by itself accelerate the learner’s backward pass. A distributed system can combine these strategies, but saying “eight GPUs” does not specify which work is split, which state is copied, or how devices communicate.'
        ]},
        {title:'Memory is not just the parameter file', paragraphs:[
          'Training memory includes parameters, gradients, optimizer state, saved activations, temporary buffers, and communication storage. In one illustrative mixed-precision Adam setup, two-byte parameters plus two-byte gradients plus a four-byte master copy plus two four-byte moments require 16 bytes per parameter before activations and overhead. A billion parameters would then need about 16 decimal GB for just those listed arrays.',
          'That is an accounting example, not a universal requirement. Some systems use different gradient dtypes, omit a separate master copy, quantize optimizer states, or shard state. ZeRO-style approaches partition optimizer state, gradients, and eventually parameters across data-parallel workers. Activation checkpointing saves memory by recomputing selected activations during backward, trading extra compute for storage.',
          'Inference has different costs: no training gradients or Adam moments are required, but KV caches grow with batch size and sequence length. MoE uses only selected experts for each token yet still needs the expert weights available somewhere. Active parameters therefore help explain compute per token, not the entire memory bill.'
        ], calculation:'Illustrative per-parameter state: 2 + 2 + 4 + 4 + 4 = 16 bytes\n1,000,000,000 parameters × 16 bytes = 16 GB (decimal)\nAdd activations, buffers, runtime overhead, and any replication.'},
        {title:'Follow the queue, not the worker count', paragraphs:[
          'Suppose four rollout workers each finish one episode every 20 seconds. They produce 4×60/20 = 12 episodes per minute. Suppose the learner can consume 8 episodes per minute. With a continuously operating producer and no discards, the pending queue grows by 4 episodes per minute. After five minutes it has gained 20 episodes.',
          'Doubling rollout workers produces 24 episodes per minute but leaves learning consumption at 8. Queue growth becomes 16 per minute. This is more generated data, not three times more learning progress. Backpressure, bounded queues, and coordinated scheduling prevent unbounded accumulation. Actual service rates fluctuate, so a full design also considers bursts and tail latency.',
          'These are deliberately stated capacity assumptions. The page does not infer GPU utilization, speedup, or cost from the worker count. Those require measurement of the actual workload. A queue can also shrink if the producer is slower than the consumer, but consumption cannot exceed what is available.'
        ], example:'queue'},
        {title:'Stale experience is not just a storage problem', paragraphs:[
          'While a rollout waits in the queue, the learner may make many updates. The current policy then differs more from the old policy that produced it. The probability ratios from the GRPO chapter can move farther from one, clipping may activate more often, and the batch may become less useful. More collected episodes can therefore mean less fresh learning signal per episode.',
          'Record policy versions with rollouts. Monitor queue age, learner lag, sample discard rates, and the fraction of updates affected by clipping. A queue length of 100 means something different when episodes last a second than when they last an hour. Do not replace age measurements with a green utilization indicator.',
          'Changing batch size also changes the optimization experiment. More workers with the same per-worker batch create a larger global batch. You may need fewer optimizer steps to process the same number of examples, but the best learning rate and generalization behavior do not follow from a universal scaling formula.'
        ]},
        {title:'Recovery and the metric that matters', paragraphs:[
          'An inference checkpoint may need only the model and its configuration. An exact training continuation needs more: optimizer moments, scheduler position, step count, random-number state, and data-order or sampler state. A distributed RL system also needs a policy-version and queue-handling plan. Restarting weights while discarding the optimizer is a different continuation.',
          'A robust save is complete and consistent across the pieces it promises to restore. Record its version and configuration, and verify a recovery path. A folder named checkpoint is not enough evidence that interrupted training resumes correctly.',
          'Ultimately compare useful outcomes at matched resource budgets. If system A costs $20 for 40 independently verified successes and B costs $18 for 20, the illustrative cost per success is $0.50 versus $0.90. The cheaper hour is not necessarily the cheaper successful task. These dollar values are invented examples, not hardware-price estimates. Throughput, reliability, and held-out quality belong in the same report.'
        ]}
      ], checks:[['Rollouts arrive at 12 per minute and the learner consumes 8. What does adding rollout workers fix?', 'Not the learner bottleneck. Without backpressure it increases queue growth and possibly policy staleness. Measure and improve the limiting stage.'],['Does an MoE with few active parameters need only those parameters in memory?', 'No. Unselected expert weights still need storage and availability somewhere. Active compute and total parameter storage are different quantities.'],['Can a weight-only file guarantee an identical Adam continuation?', 'No. Adam depends on moment history, and the run also depends on schedule, randomness, and data order. The recovery contract must say which state is restored.']],
      takeaway:'Scale the measured bottleneck, account for all state, and optimize verified progress per resource—not worker count or an attractive utilization number.',
      bridge:'You now have the pieces. The final chapter combines them into one diagnosis, then revisits the distinctions most worth remembering.', references:['map','checks'], sources:[['Megatron-LM','https://arxiv.org/abs/1909.08053'],['ZeRO','https://arxiv.org/abs/1910.02054']]
    },
    {
      id:'dashboard', title:'Put the whole system together',
      question:'Can you explain a result without confusing the model, the measurement, and the system?',
      prerequisite:'The seven preceding chapters. No implementation or coding is required.',
      outcome:'Diagnose a multi-stage experiment and explain what its evidence does—and does not—support.',
      intuition:'Conceptual understanding means you can explain mechanisms, predict consequences, and identify missing evidence—not just recognize the names of techniques.',
      sections:[
        {title:'One story, several independent choices', paragraphs:[
          'Pip began by predicting text. Attention mixed information across positions; the MLP transformed each position; next-token loss and gradients changed parameters. Instruction tuning changed the examples and target mask. LoRA offered a restricted representation of updates. Preferences supplied comparative supervision. A task environment made attempts scoreable, and GRPO used relative scores to guide probability updates.',
          'An agent harness then turned outputs into repeated actions and observations. Distributed workers changed how that work was scheduled and stored. None of these stages eliminates the need for held-out evaluation. A larger model, an adapter, a reward optimizer, and a faster cluster are mechanisms—not certificates of capability.',
          'MoE is an architectural branch, not a mandatory step between pretraining and SFT. LoRA is an adaptation choice, not a requirement before RL. DPO and reward-driven policy optimization are different approaches; neither must precede the other. The reading order builds concepts, not a universal production recipe. The recorded reference checkpoints follow their own explicitly documented lineage.'
        ], calculation:'Architecture: dense or MoE\nSupervision: text targets, demonstrations, preferences, or scored attempts\nTrainable state: full weights or adapters\nExecution: answer once or act through a harness\nSystems: distribute compute and state\nEvidence: evaluate the resulting behavior under a stated protocol'},
        {title:'Case file: the reward rises but users get worse answers', paragraphs:[
          'Read this hypothetical report: training reward rose from 0.4 to 0.9. Held-out task success fell from 60/100 to 45/100. Responses became twice as long. The team recently added a bonus for including the requested status word. They also doubled rollout workers, and the learner queue is growing. What would you investigate first?',
          'Start with the measurement. A word bonus can reward a sentence that mentions the status without satisfying the task. Compare verifier decisions on known-correct, wrong-status, and adversarial responses. Break reward into components. The rise may reflect a changed scale or a shortcut rather than better task performance.',
          'Next inspect trajectories and lengths. Is extra text earning bonus repeatedly? Is the answer format broken? Are retries more frequent? Then inspect queue age and policy versions: doubling producers without learner capacity can create stale rollouts. These are distinct hypotheses with different evidence. The report alone cannot identify a single cause.',
          'Do not respond by automatically increasing model size or loosening the evaluator. First make the independent success metric trustworthy, compare matched task sets and budgets, and isolate one change at a time. A before-and-after aggregate with several simultaneous changes cannot establish which change caused the decline.'
        ]},
        {title:'Case file: the elegant answer is not enough', paragraphs:[
          'Another report says: “Pip explains addition beautifully and passed its example.” The example was in the training set. The code evaluator only searched for addition text. The response contained that text in a comment. What can you honestly conclude?',
          'You observed fluent text and a weak evaluator accepting a familiar case. You have not established correct execution, generalization, or reliable reasoning. Run-independent correctness tests would address execution; untouched varied tasks would address generalization; inspection of intermediate claims could reveal reasoning errors. Those are different questions, so one badge should not answer all of them.',
          'This is also how to assess your own reading. If an explanation sounds familiar, ask whether you can explain the failure when one assumption changes. Recognition is a useful first step, but the ability to distinguish similar-looking mechanisms is stronger evidence of conceptual understanding.'
        ]},
        {title:'A compact set of distinctions to keep', paragraphs:[
          'Parameters versus activations: learned state across training versus computed state for an input. Training versus inference: optimizer updates versus using the current weights. Loss versus truth: fit to the stated targets versus correctness in the world. Prompt context versus prompt loss: what the model sees versus what the objective scores.',
          'Total versus active parameters: what exists versus what participates for a token. SFT versus LoRA: supervision versus update representation. Reward versus task success: an instrument’s score versus the intended outcome. Old versus reference policy: sampling baseline versus regularization anchor.',
          'Policy versus harness: the action proposer versus the system that executes and constrains actions. Throughput versus progress: work completed per second versus improvement under a trusted evaluation. Visited versus understood: opening a chapter versus being able to explain its mechanisms. Keep these pairs separate and much confusing terminology becomes easier to reason about.'
        ]},
        {title:'How to finish without writing a line of code', paragraphs:[
          'Read each review question, pause, and form an answer in your head before reading its explanation. You do not need to type, submit, or implement anything. If your explanation differs, return to the named mechanism and work through the example again. The explanations are always visible in the complete-book and print views.',
          'On a later reading, start from the case files above and try to explain them before revisiting the earlier chapters. Use the glossary when a term becomes a label without a mechanism behind it. A short return to a difficult distinction can be more useful than immediately rereading every familiar paragraph.',
          'Finishing this book should give you a connected conceptual foundation for discussing and inspecting small language-model systems. It does not establish professional expertise in implementing kernels, debugging large distributed jobs, collecting representative datasets, or operating reliable agents. Those require evidence from practice. This course deliberately teaches the reasoning that makes that practice easier to understand; it does not award an expert certificate for scrolling.'
        ]}
      ], checks:[['A LoRA run improves a trained prompt but worsens held-out tasks. Which claims are supported?', 'The adapter changed behavior and helped that trained example. Broad improvement is not supported; the held-out comparison points the other way for that evaluation population.'],['A top-2 MoE processes four tokens. Must exactly four expert assignments occur?', 'No. Before capacity limits, top-2 creates eight token–expert assignments. Overflow can remove assignments without necessarily dropping every contribution for a token.'],['Training reward improves while verified task success falls. Is GRPO necessarily broken?', 'No. Check reward definition and shortcuts, distribution changes, sampling, objective settings, and evaluation reliability. The optimizer may be faithfully improving the wrong measurement.'],['How could a system improve without any weight update?', 'Better context, tool access, harness recovery, decoding, or a larger retry budget can improve inference outcomes. State the changed resource or mechanism rather than calling every improvement training.']],
      takeaway:'Explain the mechanism, state the assumptions, inspect the evidence, and limit the claim. That habit ties the entire course together.',
      bridge:'Return to whichever distinction was hardest to explain. The glossary and full-book view remain available; progress marks are a reading aid, not a mastery score.', references:['evaluate','checks','map'], sources:[]
    }
  ];
  const glossary = [
    ['Activation','A value computed while processing an input; unlike a parameter, it is not itself a learned weight.','tinygpt'],
    ['AdamW','An optimizer using moving averages of gradients and squared gradients, bias correction, and decoupled weight decay.','tinygpt'],
    ['Advantage','A baseline-relative score used to weight a policy learning signal. Here it compares responses within one prompt group.','grpo'],
    ['Attention','A mechanism that mixes value vectors across allowed positions using weights derived from query–key scores.','tinygpt'],
    ['Autoregressive','Generating or scoring a sequence one next token at a time conditioned on earlier tokens.','tinygpt'],
    ['Batch','A collection of examples whose losses or gradients are combined for an optimization step.','tinygpt'],
    ['Causal mask','A restriction that prevents a position from attending to future positions during next-token modeling.','tinygpt'],
    ['Checkpoint','Saved model state; a resumable training checkpoint also needs the other state promised by its recovery contract.','scaling'],
    ['Clipping','In the policy surrogate here, limiting further incentive for probability-ratio changes in an already-beneficial direction.','grpo'],
    ['Cross-entropy','For a single target token, the negative natural logarithm of its predicted probability.','tinygpt'],
    ['Data parallelism','Replicating a model across workers that process different data and combine gradients.','scaling'],
    ['DPO','Direct preference optimization: learning from chosen/rejected pairs relative to a reference policy.','posttrain'],
    ['Embedding','A learned vector looked up by token ID; token IDs themselves have no numerical semantic distance.','tinygpt'],
    ['Episode','One complete environment attempt, from reset to a defined termination condition.','environment'],
    ['Expert','An MoE subnetwork selected by a router; its name does not guarantee a human-interpretable specialty.','moe'],
    ['Gradient','Local sensitivity of a loss to a parameter or intermediate value, computed through the calculation graph.','tinygpt'],
    ['GRPO','A policy-optimization approach using groups of responses to form relative learning signals.','grpo'],
    ['Harness','The system that supplies context, interprets model actions, enforces permissions, invokes tools, and stops episodes.','agents'],
    ['Held-out evaluation','Assessment on tasks kept out of training and development decisions, for a stated task population.','environment'],
    ['KL divergence','A measure of difference between probability distributions; used here to regularize policy drift.','grpo'],
    ['KV cache','Saved attention keys and values reused during generation; inference state, not an optimizer update.','tinygpt'],
    ['Logit','An unnormalized score converted into a probability by softmax. A logit is not itself a probability.','tinygpt'],
    ['LoRA','Low-rank adaptation: representing a weight update as a scaled product of two smaller trainable matrices.','posttrain'],
    ['MLP','A per-position feed-forward subnetwork; unlike attention, it does not directly mix positions.','tinygpt'],
    ['MoE','Mixture of experts: routing each token to a subset of expert subnetworks, typically replacing a dense MLP.','moe'],
    ['Parameter','A learned model value, such as an embedding or matrix entry, adjusted during training.','tinygpt'],
    ['Policy','The model’s conditional distribution over next tokens or actions given its context.','grpo'],
    ['Residual connection','Adding a sublayer’s output to its input stream, preserving a direct path through the network.','tinygpt'],
    ['Reward hacking','Earning the specified score through a shortcut that does not achieve the intended task.','environment'],
    ['RLVR','Reinforcement learning whose rewards come from verifiable task outcomes. It does not name one particular optimizer.','grpo'],
    ['Rollout','A sampled attempt or trajectory collected by a particular policy version.','grpo'],
    ['Router','A learned mechanism that scores experts and selects which process a token.','moe'],
    ['SFT','Supervised fine-tuning: training on demonstrations, commonly scoring assistant-response targets.','posttrain'],
    ['Softmax','Converting scores to positive probabilities by exponentiating and normalizing their sum.','tinygpt'],
    ['Tensor','A multidimensional array. Its shape tells you the length of each axis, not the meaning without axis labels.','tinygpt'],
    ['Token','A discrete unit from a tokenizer, such as a character, subword, or special marker.','tinygpt'],
    ['Trajectory','An ordered sequence of observations, actions, and outcomes within an attempt.','agents'],
    ['Verifier','A procedure that checks stated properties of a response or artifact; its coverage limits what a passing result proves.','environment']
  ];
  return { chapters, glossary };
});
