"""Tests for the glassbox package.

Plain ``unittest.TestCase`` classes throughout, so the suite runs under the
standard library with no third-party dependency and is also collected by pytest
unchanged.
"""
